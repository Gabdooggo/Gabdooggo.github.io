#include <iostream>
#include <cmath>

int main(){
	double x = 3.14;
	double y = 4;
	double z;
	//z = std::max(x, y);
	//z = std::min(x, y);
	//std::cout << z;
        //z = pow(2, 4);
	//z = sqrt(9);
	//z = abs(-10);
	//z = round(x);
	//z = ceil(); alwasy rounds up
	//z = floor(x) always rounds down
	//cplusplus.com/reference/cmath/


	std::cout << z;
	return 0;
}
